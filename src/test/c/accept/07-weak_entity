schema Building

entity Building {
    building_id : uuid primary
    address : string not null
}

weak entity Apartment {
    apt_number : integer primary
    floor : integer not null
    rooms : integer
    PRIMARY (Building:building_id, apt_number)
}
