schema University

entity Student {
    student_id : uuid primary
    name : string not null
}

entity Course {
    course_id : uuid primary
    title : string not null
}

relationship enrolled_in(Student many, Course many) {
    enrollment_date : date not null
    grade : decimal
}
